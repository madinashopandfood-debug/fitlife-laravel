// Not currently used — the admin panel is plain Blade + a Tailwind CDN
// <script> tag, with small inline <script> blocks per view. Kept as a
// starting point if you switch to a compiled Vite build later.
